import React from 'react';

const RepBio = (props) => {
  // console.log('repbio: ', props);
  // static info is available via props.rep
  return (
    <div>repBios: nothing!</div>
  );
};

export default RepBio;
